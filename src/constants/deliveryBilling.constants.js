const BILLING_STATUSES = Object.freeze({
  COUNTING: "counting",
  AWAITING_PAYMENT: "awaiting_payment",
  PAYMENT_PENDING: "payment_pending",
  PAYMENT_REJECTED: "payment_rejected",
  PAID: "paid",
  EXEMPTED: "exempted",
});

const DEFAULT_PRICE_PER_ORDER = 1;

const CLOSED_BILLING_STATUSES = Object.freeze([
  BILLING_STATUSES.PAID,
  BILLING_STATUSES.EXEMPTED,
]);

const OPEN_BILLING_STATUSES = Object.freeze([
  BILLING_STATUSES.AWAITING_PAYMENT,
  BILLING_STATUSES.PAYMENT_PENDING,
  BILLING_STATUSES.PAYMENT_REJECTED,
]);

module.exports = {
  BILLING_STATUSES,
  DEFAULT_PRICE_PER_ORDER,
  CLOSED_BILLING_STATUSES,
  OPEN_BILLING_STATUSES,
};
